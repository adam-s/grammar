/**
 * The fixtures, which live in `fixtures/` — one file per family.
 *
 * This file is the door. It exists so that every import of `./fixtures.ts`
 * kept working when the list outgrew one file, and it should stay empty of
 * anything but this re-export.
 */
export * from './fixtures/index.ts';
